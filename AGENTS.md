# Repository instructions

This repository contains contracts and metadata, never runtime binaries.

- Validate every non-prototype metadata entry.
- Keep artifact checksums lowercase SHA-256.
- Treat schema changes as cross-repository contract changes.
- Do not commit archives, installers, transcripts, or scratch files.
